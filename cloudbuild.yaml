steps:
- name: 'gcr.io/cloud-builders/docker'
  args: ['build', '--build-arg', 'APPDIR=geoapi', '-t', 'gcr.io/$PROJECT_ID/gojek', '.', '-f', 'Dockerfile.geoapi']
  dir: 'components'
images: ['gcr.io/$PROJECT_ID/gojek']
tags: ['geoapi-$SHORT_SHA', 'geoapi']
